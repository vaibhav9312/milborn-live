import { TouchspinDirective } from './touchspin.directive';

describe('TouchspinDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchspinDirective();
    expect(directive).toBeTruthy();
  });
});
