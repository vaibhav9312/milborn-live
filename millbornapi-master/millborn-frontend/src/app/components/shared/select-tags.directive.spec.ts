import { SelectTagsDirective } from './select-tags.directive';

describe('SelectTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectTagsDirective();
    expect(directive).toBeTruthy();
  });
});
