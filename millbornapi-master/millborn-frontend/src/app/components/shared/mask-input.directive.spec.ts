import { MaskInputDirective } from './mask-input.directive';

describe('MaskInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskInputDirective();
    expect(directive).toBeTruthy();
  });
});
