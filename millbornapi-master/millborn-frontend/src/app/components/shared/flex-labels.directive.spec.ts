import { FlexLabelsDirective } from './flex-labels.directive';

describe('FlexLabelsDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLabelsDirective();
    expect(directive).toBeTruthy();
  });
});
